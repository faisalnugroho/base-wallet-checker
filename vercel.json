{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/api-server build",
  "outputDirectory": "artifacts/api-server/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.mjs" }
  ]
}
